query getProfile {
  dataJson {
    jobs {
      current {
        name
        position
        url
      }
      history {
        main {
          name
          period
        }
        side {
          name
          period
        }
      }
    }
    sns {
      service
      url
      color
    }
  }
}
